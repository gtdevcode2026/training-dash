# Growth Group

Dashboard section ID: `growth`  
LMS training name: Mandatory Anti-Phishing Training — Growth Group #ABI

## Folders

| Folder | Purpose |
|--------|---------|
| `input/` | Place Datamart and weekly LMS exports here before running scripts |
| `output/` | Dated Excel outputs from CLI scripts |
| `processed/` | Dated snapshots of inputs used per run |
| `samples/` | Optional reference workbooks (not used automatically) |
| `scripts/` | Training-specific automation |

## Browser dashboard

Open `../../dashboard/index.html` and use the **Growth Group** tab.

## CLI (Python)

This training is processed in the browser dashboard only (no separate CLI script in the legacy `trainings` repo).
